{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "cd ../.. && cd packages/toolkit && bun run build && cd ../../services/mcp-server && bun run build",
  "installCommand": "cd ../.. && bun install --frozen-lockfile",
  "regions": ["iad1"],
  "functions": {
    "app/api/**/*.ts": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}
