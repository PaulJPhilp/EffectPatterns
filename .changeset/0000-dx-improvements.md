---
"@effect-patterns/ep-cli": patch
---

DX Improvements:
- Added global `--verbose` and `--log-level` flags for better logging control.
- Fixed `Logger` service injection issue that caused crashes.
- Added descriptive metadata to `search` and `list` commands for better help output.
- Confirmed shell completion support via `--completions` flag.
