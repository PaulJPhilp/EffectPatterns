---
"@effect-patterns/ep-cli": minor
---

Feature: Interactive rule selection for `ep install add`
- Added `--interactive` (`-i`) flag to `install add` command.
- Users can now select specific rules from a list using a multi-select prompt.
- Retained existing non-interactive filtering behavior.
