{
  "mcpServers": {
    "effect-patterns": {
      "type": "stdio",
      "command": "bun",
      "args": [
        "run",
        "--cwd",
        "packages/mcp-server",
        "mcp:debug"
      ],
      "env": {
        "PATTERN_API_KEY": "${PRODUCTION_API_KEY}"
      }
    }
  }
}
